<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand">DynaForm</a>
      <ul class="navbar-nav me-auto mt-2 mt-lg-0">
        <%= render partial: "nav_item", locals: {href: published_surveys_path, label: "Published Surveys"} %>
        <% if Current.user %>
          <%= render partial: "nav_item", locals: {href: dashboard_path, label: "My Dashboard"} %>
        <% end %>
      </ul>
      <div class="navbar">
        <ul class="navbar-nav ms-auto mt-2 mt-lg-0">
        <% if Current.user %>
          <li class="nav-item">
            <%= button_to "Logout", logout_url, class: 'form-control', method: :delete %>
          </li>
        <% else %>
          <li class="nav-item px-3">
            <%= button_to "Login", login_url, class: 'form-control nav-item', method: :get %>
          </li>
          <li class="nav-item">
            <%= button_to "Sign up", signup_url, class: 'form-control nav-item', method: :get %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>